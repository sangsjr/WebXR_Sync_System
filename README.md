# WebXR Sync System

The WebXR Sync System is designed to achieve positional synchronization between mobile devices and Meta Quest headsets, enabling real-time tracking of the mobile device's position by the headset while browsing various web pages. This repository contains two implementations of the system:

* web branch: Web version

* plugin branch: Extension version
